<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
       http://dubbo.apache.org/schema/dubbo
       http://dubbo.apache.org/schema/dubbo/dubbo.xsd">
    
    <!--&lt;!&ndash; dubbo配置_当前应用名，不能带"." &ndash;&gt;-->
    <dubbo:application name="${dubbo.appName}" owner="${dubbo.appOwner}" logger="slf4j"/>

    <!-- dubbo配置_注册中心 -->
        <dubbo:registry id="register" address="${dubbo.register.address}"  timeout="20000"  file="${dubbo.cache.file}"/>
<!--    <dubbo:registry id="register" address="zookeeper://test-zk.yangtuojia.com:2184?backup=test-zk.yangtuojia.com:2185,test-zk.yangtuojia.com:2186"  timeout="20000"  file=" /tmp/hirac-dubbo-registry.cache"/>-->
    <dubbo:registry id="metaRegister" address="zookeeper://test-zk.yangtuojia.com:2184" timeout="300000" file="/tmp/hirac-dubbo-registry-meta.cache"/>

    <!-- 服务消费全局配置 -->
    <dubbo:consumer check="false" timeout="10000" retries="0"/>

</beans>
